import RegisterForm from '@/components/login/RegisterForm';

export const metadata = {
  title: 'Register - AI CV Fit Analyzer',
  description: 'Create an AI CV Fit Analyzer account.',
};

export default function RegisterPage() {
  return <RegisterForm />;
}
